# AI Agent 專案指示

## 適用範圍與優先順序

- 本檔適用於整個專案目錄。
- 使用者當次明確指示優先於本檔。
- 本檔只保留長期穩定的工作規則；歷史交接、已完成需求與暫時性限制不應放在這裡。
- 若本檔與專案現況明顯衝突，先指出衝突並依使用者當次目標提出最小修改方案。

## 工作方式

- 先理解使用者目標，再決定是否需要讀檔、搜尋或修改程式。
- 進行較大或多步驟任務時，先列出簡短計畫；過程中保持計畫可更新。
- 回答與修改都應聚焦於當次任務，不要因本檔而主動擴大範圍。
- 避免過度保守：若專案內已有明確資料，可依專案資料實作合理預設與流程。
- 若專案內資料不足、互相矛盾，或修改會改變規則結果，先列出矛盾、建議正規值與需要使用者決定的問題。

## 測試限制

- 禁止使用瀏覽器控制、瀏覽器自動化、Playwright 或其他瀏覽器操作測試本專案。
- 修改 JavaScript 後只需執行語法檢查，例如 `node --check <檔案>`。
- 不需要為了 UI 畫面或互動流程另外啟動本機伺服器或瀏覽器。

## 離線角色卡

- 除非使用者在當次要求中明確要求建立或更新離線角色卡，否則禁止執行 `build-offline-nopdf.ps1`。
- 一般修改只需更新來源檔案，不必同步重新產生 `TWD20-offline.html`。

## 規則與資料來源

- 本網頁專案內的文本、選項與現有程式資料是創角精靈最高優先的設計標準。
- 語言模型記憶或網路搜尋到的 D&D 規則只能當作參考，不得用來覆蓋或擴充專案內文本。
- 未經使用者當次明確要求，不需為了 D&D 規則內容上網查詢。
- 當專案內不同檔案的文本或名稱互相矛盾，先列出矛盾與建議正規值；若會改變規則結果，必須先請使用者決定。
