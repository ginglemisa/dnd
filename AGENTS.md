# AI Agent 專案指示

## 適用範圍

- 本檔適用於整個專案；使用者當次明確指示優先。
- 本檔只保留長期穩定規則，不記錄歷史交接、已完成需求或暫時性限制。
- 修改應聚焦當次任務，避免無關重構或主動擴大範圍。
- 若本檔與專案現況衝突，指出衝突並採取能完成當次目標的最小修改。

## 工作方式

- 優先搜尋與讀取和任務直接相關的檔案；證據不足時再擴大範圍。
- 低風險、可逆且符合現有模式的細節可自行採用合理預設。
- 若不同來源互相矛盾，且選擇會改變規則、資料意義或對外行為，列出差異與建議值後交由使用者決定。
- 較大任務可維持簡短工作計畫；除非有助於協作，否則不必額外輸出計畫。

## 測試

- 本專案為原生 HTML、CSS、JavaScript 靜態網頁；不要為一般修改新增測試框架、套件管理器或開發依賴。
- 預設不得使用瀏覽器控制、Playwright 或其他瀏覽器自動化。
- 修改 JavaScript 後，優先對受影響檔案執行最低成本且現有環境可用的檢查，例如 `node --check <檔案>`。
- 一般修改不執行 frontend-design-premium 的全專案 `audit_project.py` 或 `designmd lint`；除非使用者當次明確要求，應以本專案相關的最低成本檢查取代，避免離線產物與靜態事件判定造成無關結果。
- 除非使用者明確要求，或問題無法透過非瀏覽器方式確認，否則不啟動本機伺服器或瀏覽器。
- 使用瀏覽器時，只驗證受影響的最小流程，不做全面巡覽。

## 離線角色卡

- 除非使用者當次明確要求建立或更新離線角色卡，否則禁止執行 `build-offline-nopdf.ps1`。
- 一般修改只更新來源檔案，不需同步重新產生 `TWD20-offline.html`。

## 規則與資料來源

- 專案內現有文本、規則與選項資料是主要 source of truth，不得以模型記憶或網路資料自行覆蓋或擴充。
- 角色計算與共用規則以 `character-rules.js` 為準；職業、種族、背景、專長與工具資料分別位於 `class-features.js`、`race.js`、`backgrounds.js`、`feats.js`、`tool-data.js`。
- 其他會影響角色選項或顯示內容的資料，分別位於 `monster.js`（野獸／怪物）、`equipment-data.js` 與 `equipment-notes.js`（裝備）、`spell-list.js`（法術）、`condition.js`（狀態）及 `deity-info.js`（神祉）。
- `action-panel.js` 負責「動作」分頁的基本與動態按鈕；動態按鈕從職業、種族、專長、已選法術與魔能祈喚的既有資料萃取，僅在此檔處理按鈕的顯示條件、標題與說明呈現，不得複製或取代來源規則文字。標題帶有「等級 X：」的動態按鈕必須受角色等級限制；種族血統或祖源能力也必須受目前下拉選項限制。
- Legal & About 的說明、授權與 attribution 文字以 `about.html` 為唯一來源；其獨立頁面樣式以 `info-pages.css` 為準。`legal-modal.js` 只負責首頁 iframe modal 的開關與定位，不得重複保存同類文案。
- `index.html` 內的初始化、選項串接與角色卡行為，以及 `quick-build.js` 的創角小幫手流程，都必須與上述規則／資料來源一致；若有衝突，不應僅以既有程式行為判定正確。
- 未經使用者當次明確要求，不需為 D&D 規則內容上網查詢。
- 專案內來源若互相矛盾且會改變角色規則或計算結果，列出差異與建議正規值後交由使用者決定。
