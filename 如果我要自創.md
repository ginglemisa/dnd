# 如果我要自創

這是一份給使用者與 AI 協作的私人擴充指南，適用於提高等級上限、種族、職業與子職、背景、專長、戰鬥風格、傳奇恩惠、法術與戲法，以及它們帶來的動作與資源。

目標是完成指定內容的可用版本；不必一次補齊所有職業到 20 級，也不必先建立通用 homebrew 編輯器。**新增說明文字、讓選項可選、正確計算、跑團模式可操作、能保存還原，是不同工作，必須逐項確認。**

**LV1 創角小幫手流程不在自創工作範圍內。** 自創內容直接透過角色卡設定，不需加入小幫手選項或擴充其流程。但角色卡也會呼叫 `quick-build.js` 的 `openSpellDetail`／`openSpellPrepareDetail`；涉及法術詳情或準備操作時，仍須追查並驗證這些共用呼叫，不把整個檔案視為無關。

檔名與函式名稱是搜尋入口；實作前確認實際呼叫路徑。通用工作限制見 [AGENTS.md](AGENTS.md)，驗證命令與環境見 [README：維護與驗證](README.md#維護與驗證)。

## 1. 使用者最快的開始方式

複製下列文字給 AI，填入確定的部分即可。可以先只提供創意，請 AI 整理規則草案；也可以直接提供已定稿的規則，要求實作。

```text
請閱讀 AGENTS.md 與「如果我要自創.md」，在目前工作區完成以下私人擴充。

用途：私人使用，不上傳、不部署；不要自行 commit 或 push。
內容類型：種族／職業／子職／背景／專長／戰鬥風格／傳奇恩惠／法術／戲法／等級擴充。
名稱與概念：
規則文本或來源：
規則是否定稿：已定稿／請先協助設計。
規則版本：本專案既有版本／指定版本／完全自訂。
目標等級：例如只讓戰士及這個子職支援 1～12 級。
加入方式：保留原選項並新增／替換私人版中的指定選項。
需要的介面：角色卡、跑團模式；是否也要 PDF、單檔離線版？
排除範圍：不擴充 LV1 創角小幫手流程；角色卡使用的共用功能仍依影響檢查。
自動化要求：哪些要自動計算或扣資源，哪些接受文字提示與手動操作？
現有角色：要保留哪些 JSON／存檔的還原相容性？
外部查詢：本次是否允許查網路？如允許，限哪些規則與來源？

先檢查現有實作，整理規則、影響範圍與驗收案例，再持續完成修改及適用驗證。
不要把新增資料當成自動化已完成。只詢問真正影響玩法、規則或資料相容性，
而現有資料又無法判定的問題；其餘沿用既有設計。
交付時列出完成項目、手動項目、測試結果與尚未支援的範圍。
```

只有概念時，先產出可討論的規則草案與相對於既有選項的強弱差異。尚未決定的核心玩法不要由 AI 默默定案；已提供完整規則並要求實作時，不必每個階段再要求批准。

## 2. AI 開工前的工作邊界

1. 先讀 [AGENTS.md](AGENTS.md)，確認 git status、目前分支與使用者既有修改，只改本次必要內容。
2. 區分「目前程式怎麼做」與「新內容應該怎麼運作」。前者依 repository 確認，後者依使用者指定規則；不要把尚未支援誤認成玩法禁止。
3. 規則以專案資料及當次提供的文本為準；外部查詢須依當次明確授權，不把閱讀本指南視為查網路的授權。
4. 完全自創的規則清楚標為自創；官方內容、改編內容與自創內容不要混稱。不要依模型記憶補出未提供的升級表，也不要混用不同版本的同名能力。
5. 沿用原生 HTML／CSS／JavaScript 與傳統 script 載入。不為單一擴充引入框架、build pipeline、第三方 runtime、中央 store 或另一套擲骰／儲存系統。
6. 只要使用者沒有要求泛用編輯器，優先直接擴充現有資料及必要串接。多個新增內容確實共享同一需求時，才做小範圍的資料化整理。
7. 若只支援部分職業到新上限，等級選單與匯入檢查也要反映支援範圍；不要讓未補完的職業看起來也完整支援。
8. 不因 LV1 創角小幫手沒有新增自創選項而阻擋交付；角色卡對其共用函式的依賴依實際影響處理。

私人版可使用獨立本機副本，或沿用既有 task branch／worktree；不需要另開公開 repository。分支本身不是防止上傳的機制。若另建私人副本，可不設定推送遠端，並與正式版使用不同且固定的本機 origin，以隔離 LocalStorage；同 origin 只換路徑不會隔離 LocalStorage。

## 3. 先把規則變成可實作的規格

每個新選項至少整理以下欄位。這是設計清單，**不是專案已存在的統一 schema 或 API**；實作時對應到現有資料結構。

| 欄位 | 必須說清楚的內容 |
| --- | --- |
| 身分 | 穩定 key／ID、顯示名稱、內容類別、來源與規則版本 |
| 取得方式 | 主職業／子職／背景／種族／升級選擇／DM 獎勵；使用哪一種選擇額度 |
| 資格 | 角色等級、職業等級、屬性、前置能力、裝備或角色選擇條件 |
| 選項 | 選幾項、能否重複、互斥條件、何時可更換、移除來源後怎麼處理 |
| 效果 | 數值公式、取整、上下限、是否替換原效果、是否可疊加 |
| 使用時機 | 動作／附贈／反應／移動／被動／觸發式；每回合或每次攻擊限制 |
| 資源 | 上限、消耗、恢復方式、共享或獨立資源、使用取消的處理 |
| 成長 | 哪些等級改變骰數、範圍、次數、法術位、已知或準備數量 |
| 自動化 | 顯示提示、追蹤資源、自動算數值、自動擲骰或治療，各自是否需要 |
| 保存 | 角色選擇、剩餘資源、開關、玩家偏好各自保存在哪裡 |

提高等級或新增職業時，先整理逐級表：

| 等級 | 新增／替換能力 | 選擇額度 | 資源上限 | 骰式與數值 | 法術位／準備數 | 預期驗收 |
| --- | --- | --- | --- | --- | --- | --- |
| 取得前一級 | 不應出現新能力 | 舊額度 | 舊值 | 舊公式 | 舊值 | 不提前生效 |
| 取得當級 | 指定能力 | 新額度 | 新值 | 新公式 | 新值 | 正確生效一次 |
| 下一變化級 | 升級或替換 | 依規則 | 依規則 | 依規則 | 依規則 | 不重複疊加舊版 |

不要從上面的空表猜規則值。無法自動判斷的戰場條件，例如敵人是否在範圍內，可由玩家確認並提供明確提示；不要宣稱程式已驗證它沒有保存的戰場狀態。

## 4. 專案入口與資料流

| 範圍 | 優先入口 | 應追到的使用端 |
| --- | --- | --- |
| 共用計算 | [character-rules.js](character-rules.js) | 主頁數值、動作摘要、跑團模式、PDF 是否沿用或另有摘要 |
| 職業／子職 | [class-features.js](class-features.js) | 主頁職業選項、能力分級顯示、自動效果、衍生法術與資源 |
| 種族 | [race.js](race.js) | 血統／體型／速度／能力、種族法術、動作與資源 |
| 背景 | [backgrounds.js](backgrounds.js) | 背景專長、技能工具、屬性與初始裝備 |
| 專長／戰鬥風格 | [feats.js](feats.js) | 選項分類、衍生專長列、資格、戰鬥效果、特殊動作 |
| 法術／戲法 | [spell-list.js](spell-list.js) 的 `SpellCatalog` | 職業清單、環階、搜尋、準備、施法 metadata、法術書 |
| 主要 UI 與保存 | [index.html](index.html) | 選單、初始化、事件、LocalStorage、JSON、分享、PDF 載入 |
| 非法術動作 | [action-panel.js](action-panel.js)、[tabletop-actions.js](tabletop-actions.js) | 穩定 key、分類、條件、顯示與操作 |
| 跑團模式資源與休息 | [character-rules.js](character-rules.js)、[tabletop-mode.js](tabletop-mode.js)、[tabletop-resources.js](tabletop-resources.js) | 資源定義、消耗、短休／長休、生命骰、保存與同步 |
| 跑團模式施法／法術書 | [tabletop-spells.js](tabletop-spells.js)、[spellbook.js](spellbook.js) | 扣位、免費施法、升環、專注、儀式、擲骰與準備總數 |
| 德魯伊／野獸 | [tabletop-druid.js](tabletop-druid.js)、[monster.js](monster.js) | 形態資格、野獸攻擊、資源、施法限制與保存 |
| PDF | [pdf-field-map.js](pdf-field-map.js)、[pdf-export.js](pdf-export.js) | 名稱、數值、能力摘要、法術列及實際版面 |

基本追查順序：**資料定義 → 可選入口 → 角色選擇 → 計算／衍生項目 → 跑團模式操作 → 保存還原 → 輸出**。沿這條路找缺口，比搜尋名稱後只改文字可靠。

`character-rules.js` 現況以多個函式掛到 `window`；請確認實際公開介面，不要只因架構文件寫「CharacterRules」就假設存在同名物件。表單與結構化 DOM 可繼續作為目前選擇的來源。

## 5. 實作順序與完成程度

1. **建立規格及驗收案例。** 確定支援等級、加入或替換、介面範圍及哪些效果由玩家處理。
2. **補資料與可選入口。** 新選項有唯一身分、能顯示說明，舊選項不受影響。
3. **接上條件與計算。** 除取得當下外，也處理升降級、切換來源、裝備改變與選項移除。
4. **接上動作及資源。** 使用 `TabletopMode` 管理共用狀態，`DiceRoller` 處理擲骰與歷史；dialog／toast 用 `app-dialog.js`。內建資源從 `getCharacterResourceSpecs` 定義上限、`target`、`recovery` 與需要的 `restChoice`，由 `getRestContext`／`commitRest` 結算休息；只寫恢復說明不會自動接上恢復操作，自訂資源的 `recoveryNote` 也只是文字。
5. **完成保存及所需輸出。** 新資料可還原，舊角色有合理預設，PDF 或離線版只在需求包含時處理。
6. **執行相關驗證並收尾。** 更新被修改的正式載入 JS／CSS 在 `index.html` 的快取版本，檢查 diff、git status 與暫存檔。

可以分階段交付，但須明確區分「僅顯示」「可追蹤使用次數」「可自動計算／執行」。若使用者要求完整實作，不能以第一階段可看見文字就宣告完成。現有自訂動作與自訂資源可用於先試玩規則；自訂動作主要為文字說明，不能等同完整能力實作。

## 6. 各類自創的實作大綱

### 6.1 提高等級上限

- 同時調整 `index.html` 的 `#level` 與 `#lifedicen`。檢查切換至較低上限職業時的處理，不要靜默截斷匯入角色。
- 補 `class-features.js` 的升級表與 `data-feature-level` 能力區塊，確認 `getClassFeatureHtmlBeforeLevel`／`updateClassFeature` 能正確分級顯示。
- 補 `character-rules.js` 的準備數、各類法術位、資源、武藝骰、速度、祈喚／超魔數量等；補主頁 `getEntitledManualFeatRowCount` 及新增選擇欄位。
- 熟練加值已有 1～20 級分段，不代表其他進程已有支援。若要超過 20 級，先定義自創的完整成長規則，不把現有公式無限外推。
- 分別核對角色等級、職業等級、法術環階；目前主要 UI 為單一職業與等級，提高上限不等於增加兼職系統。
- 現況 HP 的 `clampHpValue` 與 `#hp` 上限是 200；六項屬性輸入上限為 20，事件會依欄位上限截值；法術 DC 欄位也有 `max="20"`。檢查新規則是否會超過，並同步查看匯入、自動計算、跑團模式與 PDF。
- 若某能力允許屬性超過 20，分開處理「可輸入的角色最終值」與「特定加值來源允許提高到多少」。不要把 27 點購買的基礎值上限 15 或所有普通專長的上限一起放寬。
- 高等級也可能影響種族能力、戲法、專長與既有低環法術的升環結果；不能只測新職業能力。

### 6.2 種族與血統

- 在 `race.js` 加入描述，並更新主頁種族選單；不要假設資料物件新增後選單會自動出現。
- 定義生物類型、可選體型、速度、感官、抗性、語言與各級特質；只對需要自動化的項目接計算。
- 檢查 `getCharacterSizeForRace`、`normalizeCharacterSize`、`calculateCharacterSpeed`、HP 加值及施法能力判定。現況體型只接受小型／中型；新增其他體型要同步考慮相關 UI 與規則。
- 有血統等次選擇時，建立穩定控制項與選擇條件，清除或停用已失效的衍生效果。
- 種族法術追到 `RACE_DERIVED_SPELLS`、`getRacialDerivedSpellSpecs`：取得等級、施法屬性、免費次數、是否可用法術位、是否始終準備，各自明確定義。
- 主動能力接 `RACE_ACTION_RULES` 與資源；需要野獸／召喚物資料時再查 `monster.js`，不要複製另一套攻擊計算。
- 驗收至少包含新種族 → 舊種族 → 新種族，以及血統切換後法術、速度、HP、資源與保存的結果。

### 6.3 主職業與子職

先決定：新增主職業、私人版替換固定子職，或保留原子職並新增可切換子職。

- 新主職業需補主頁選單、`classFeatures`、生命骰、豁免熟練、施法判定／屬性／清單、資源與相關能力；技能工具、初始裝備及 1 級能力依需求在角色卡呈現或由玩家設定。
- 現況每個主職業固定一個子職；子職文本、自動效果與衍生法術混在主職業路徑內。新增切換子職時，建立穩定 `subclass` 選擇與主職業對應，不要只替換標題。
- 對每個子職能力加上實際子職條件，涵蓋 `CLASS_ACTION_RULES`、資源、自動數值、`SUBCLASS_DERIVED_SPELLS`、跑團模式施法修正與 PDF 名稱。
- 特別檢查原能力殘留：術士龍族子職的 HP／AC、牧師生命子職的治療加值、戰士勇士的額外戰鬥風格、德魯伊大地結社的法術與恢復，目前有直接按主職業判定的路徑。
- 舊角色沒有子職欄位時，依原固定子職還原；新角色在尚未取得子職時不能提早生效。換子職後保留或清除哪些玩家選擇，依規格處理。
- 還原順序先有主職業、等級與子職，再建立其控制項及衍生項目，最後套用細項；確認事件不會在中途把待還原值清掉。

若採替換方案，保留相同主職業 ID 會讓舊角色在私人版下得到新的子職意義；這是需要明確說明的相容性選擇。不能把它描述為舊角色規則完全不變。

### 6.4 背景

- 補 `detailedBackgroundFeatures` 與主頁背景選單；定義屬性候選、起源專長、技能、工具、裝備／金幣方案及說明。
- 主頁 `getDerivedFeatSpecs` 目前以背景 `專長` 文字的開頭比對 `FEAT_OPTIONS.value`。新命名不得意外撞到其他名稱；若需求超過此機制，為受影響資料加入明確對應並保留舊格式相容。
- 檢查 `updateBackgroundFeature`、工具列、魔法學徒等衍生法術及施法能力判定。
- 在角色卡核對背景帶來的技能／工具熟練、屬性與裝備選擇，並確認是否重複套用。
- 驗收背景切換、固定專長列移除、工具／法術更新，以及使用者另外選取的項目不被誤刪。

### 6.5 專長

- 現有 `featsDesc` 是說明，`FEAT_OPTIONS` 是選單資料；兩者都要補。現有 `value` 常直接用中文名稱，已進入保存資料，不要為了格式統一全面改成英文 ID。
- 指定分類、取得來源、等級／屬性／能力前置、是否可重複，以及加值的選擇與上限。
- 一般下拉選單目前直接列出 `FEAT_OPTIONS`；在說明寫「19 級以上」不會自動產生完整資格限制。要自動限制時，新增明確條件並處理已選項因降級而失效的狀態。
- `hasSelectedFeat` 與特定名稱判斷控制部分效果；新增描述不會自動給予 HP、AC、速度、攻擊或屬性加值。
- 有動作時補 `TABLETOP_FEAT_ACTION_RULES`；有資源時接共用資源；有法術時沿用衍生法術機制。
- 屬性提升若要自動套用，先確認主頁能力值的既有保存意義。重新整理、重新選擇或匯入不得重複加值；取消選擇也不能直接減去一個不知來源的數字。必要時保存加值來源，或明確採用玩家手動調整。
- 驗收資格不足、取得、重複、取消、降級與 JSON 還原；可重複專長還要驗證每次選擇的額外限制。

### 6.6 戰鬥風格

- 沿用專長系統，但取得資格來自指定能力／規則，不是任意職業都自動得到一次選擇。
- 目前 `getFightingStyleOptionsHtml` 從 label 的「戰鬥風格」文字辨識類別。新增分類時保持既有標籤相容，或同步更新自創範圍內受影響的使用端。
- 檢查 `getDerivedFeatSpecs` 的取得等級、選擇數量、職業／子職條件，以及多次取得時能否選相同風格。
- 接上武器主手／副手、遠近程、盾牌、護甲、雙手／單手及傷害骰等實際條件；不要把某風格效果無條件加到所有攻擊。
- 核對 `character-rules.js`、主頁武器自動化、`tabletop-actions.js`、`DiceRoller` 及 PDF 的數值一致性。改擲骰處理時保留結果與歷史的單一入口。

### 6.7 高環法術與戲法

先分清楚三件事：新增法術資料、讓角色取得／準備它，以及讓角色有合法施放方式。三者不能互相代替。

- 在 `spellsById` 定義唯一 `spellId`、中英文名、環階及描述，在 `fullSpellIdsByClass` 補職業關聯；不要另建一份 UI 專用法術資料。
- 描述沿用既有欄位格式，完整寫施法時間、射程、成分、材料、持續時間、專注、效果與升環。專注與部分顯示仍依描述格式解析，不能任意換格式而不查使用端。
- 需要儀式、升環或自動結果時，補 `ritualSpellIdSet`、`upcastSpellIdSet`、`outcomeMetadataBySpellId` 等對應資料；戲法另查 `TABLETOP_DAMAGE_CANTRIP_IDS` 與 `canCastFromTabletop`，確認按鈕真的出現。
- 新增五環以上需補 `getSpellSlotCounts`、`classCanAccessSpellLevel`、主頁法術位控制項、`SPELL_RING_CONFIGS`／法術容器、跑團模式法術位群組與 `scroll-to-top.js` 的環階入口；不要只把一處 `4` 改成 `9`。
- 同步檢查 `tabletop-mode.js`、`tabletop-resources.js` 中固定 `[1,2,3,4]` 的迴圈，以及 `spellbook.js` 的環階標籤、準備與儀式操作。
- 法術位容量、準備數量、可用環階、學會／法術書規則分開補；契術師的契約法術位與任何獨立高環能力依指定規則分開表達，不假定都走一般施法進程。
- 戲法成長要明確標註看角色等級還是職業等級。核對傷害骰、重複攻擊次數、每次／一次附加值、目標數，以及主頁摘要、跑團模式擲骰與 PDF；現有 `cantripAt5`／`repeatAt5` 不會自動支援後續成長。
- 高環位也會用於低環法術升環：檢查骰式、固定值、目標數及「無額外升環效果仍能合法使用高環位」的情況。
- 持續影響 AC／速度等數值的法術，追查 `TabletopMode.setPersistentSpellEffect` 與現有持續效果定義，處理目標、疊加、解除、休息、保存及荒野形態；加入術文或擲骰 metadata 不會自動套用持續效果。
- 對召喚、傳送、控制或劇情效果，不必強行做成傷害骰；按已同意範圍完成扣位、專注與提示，其他效果由玩家處理。
- 驗收搜尋 → 加入 → 準備 → 施法 → 選環位 → 扣資源 → 專注／結果 → 取消或還原；法術書與始終準備來源另外測。

### 6.8 傳奇恩惠（Epic Boon）

本專案目前沒有傳奇恩惠資料或專門取得流程。請提供要採用的完整規則，分開定義取得來源、資格、選擇額度與屬性上限；提高上限不等於直接把屬性設成上限值。

建議實作順序：

1. 先確認用途：按指定版本的升級獎勵、自創較低等級恩惠，或 DM 額外獎勵。取得等級、額度與前置豁免以當次規格為準，不偷偷替使用者選一種。
2. 優先擴充既有專長資料及選擇流程；不要因分類不同就另建一套互不相通的儲存或能力系統。
3. 為新分類提供明確的類別、資格、可重複性與取得來源資訊。這些是需要實作的資料，不是目前已存在的通用專長 metadata。
4. 若由升級取得，補該職業的選擇節點，避免同一次獎勵同時多送普通專長與恩惠。若由 DM 另外授予，保存獨立來源，不能只因不符合一般升級門檻就抹掉已授權的自創獎勵。
5. 處理屬性選擇與上限，保留普通來源自己的上限；驗證超過 20 的最終值能經過輸入事件、重整及匯入而不被截斷。
6. 每個恩惠分別拆成被動效果、可操作能力與資源。回合開始、擲先攻或特定事件恢復，不可一律改成短休／長休；系統未追蹤該事件時提供明確手動恢復操作。
7. 涉及命中改寫或不消耗法術位時，要明確設計玩家確認、資源處理與擲骰紀錄的先後；不重複扣位、返還或發動。
8. 驗收門檻前後、取得來源、互斥／重複、屬性上限、使用與恢復、取消選擇及保存還原。若目標只到 12 級且未要求低等級恩惠，不必預先實作整個系統。

### 6.9 其他自創內容

魔能祈喚、超魔、職業次選項、工具、裝備、召喚物等仍沿第 3 節規格與第 4 節資料流處理。裝備優先查 [equipment-data.js](equipment-data.js)／[equipment-notes.js](equipment-notes.js)，工具查 [tool-data.js](tool-data.js)，狀態查 [condition.js](condition.js)。只延伸任務實際需要的路徑。

## 7. 保存、相容性與私人輸出

- 沿用 `window.dndStorage`、`collectStateObject`、`applyStateObject` 與 `TabletopMode` 的保存介面。一般有 ID 的控制項可被收集，不代表動態控制項的建立順序與還原已自動正確。
- 保留既有 ID、法術 ID、動作 key 與資源 key；改顯示名稱不應改身分。替換內容若語意已不同，要決定遷移方式，不重用舊 key 偽裝同一能力。
- 新欄位缺失時有明確預設；未知新 ID、超出支援等級或私人版版本不符時，提示並保留可回復資料，不靜默變成別的選項。只有實際需要時才加入私人內容版本標記與遷移。
- 區分角色資料與瀏覽器偏好。跑團模式自訂動作、隱藏偏好、動作筆記目前獨立保存，不包含在角色 JSON／分享網址；備份需求若包含它們，需另外處理，不能宣稱角色 JSON 已涵蓋。
- JSON 與分享網址主要保存角色狀態，不會附帶新增職業／法術的程式與規則。還原端需要相容的私人版本。網頁版永久分享網址使用目前 origin／path／query；離線產生腳本則固定分享至 `https://twd20.com`，既有 QR Code 也指向正式站。這些正式站入口無法還原尚未收錄的自創內容；私人版優先用 JSON 搭配相同版本程式交換，若要支援私人分享再明確調整目的地。
- 私人版來源也要備份。不上傳不等於自動封鎖網路；若使用者另外要求完全不連外，再檢查 Analytics 等外部請求與資源，不能把一般本機開啟宣稱為完全離線。
- PDF 另查固定的職業／子職名稱、骰式摘要、法術位欄位與法術列容量。現況 `MAX_PDF_SPELL_ROWS` 為 19，超出的法術會整理到備註，法術位映射到四環；若要更多完整法術列或高環欄位，先檢查模板與實際輸出版面，再決定映射或增加頁面。
- 只有當次明確要求建立／測試／更新單檔離線版時，才執行 [build-offline-nopdf.ps1](build-offline-nopdf.ps1)。來源檔是修改入口；不要只改 `TWD20-offline.html`。離線精簡版停用 PDF。

## 8. 驗證清單：先證明新規則，再確認舊功能

### 8.1 靜態與規則檢查

- 對照規格逐項確認資料存在、ID 唯一、取得條件、取整、上限、重複與替換關係。
- 用 `rg` 搜尋新舊 ID、名稱、相關公開 API 與 DOM ID，追查所有使用端；搜尋結果中的數字可能是骰面、屬性或排版，不做全域數字替換。
- 每個變化節點測「前一級／當級」，再測 1、8、9、目標最高等級；11、13、17、19、20 等只在支援範圍及相關規則需要時納入。
- 直接呼叫純規則函式，比對事先寫好的預期值。不要把實作算出的結果直接當成測試答案。
- 對受影響 JavaScript 執行 `node --check`；若改 `index.html` 的 inline JavaScript，另確認其 script 內容語法與實際初始化，不能以外部檔案檢查代替。

### 8.2 最小必要 UI 流程

1. 新建角色選到新內容，看到正確說明、數值、選項與資源。
2. 在能力門檻前後升降級；切換主職業／子職／種族／背景再切回，確認舊效果不殘留、新效果不重複。
3. 有條件效果時換裝備或調整必要選擇，確認只在符合條件時生效。
4. 使用、取消、用盡、恢復資源；新法術加測環位、升環、免費使用、專注與擲骰。
5. 重整確認 autosave；匯出 JSON 後從乾淨狀態匯入，再載入舊角色。不能只在原頁面上匯入原資料，掩蓋缺少還原的欄位。
6. 若有支援分享，驗證同一私人版的分享還原及分享模式不覆寫本機角色；若需要 PDF／離線版，驗證實際產物。
7. 新 UI 檢查窄版、長名稱、鍵盤焦點與對話框；DOM／文字足以判定時不截圖，真正版面問題才看相關區域 screenshot。

臨時瀏覽器驗證前讀 [.agents/skills/playwright-cli/SKILL.md](.agents/skills/playwright-cli/SKILL.md)，沿用 `npx --no-install playwright cli` 與既有依賴；完成後關閉此次 browser session 與本機伺服器。

### 8.3 依影響選擇現有 regression scripts

完整腳本清單與用法見 [README：維護與驗證](README.md#維護與驗證)，必跑條件見 [AGENTS.md：驗證條件](AGENTS.md#驗證條件)。新增資源恢復需涵蓋休息驗證；持續法術效果需涵蓋德魯伊驗證的一般／野獸狀態；PDF 與離線輸出依需求選用對應腳本。種族／背景沒有完全對應的專用測試時，補測其實際資料流。

現有驗證的邊界：

- 動作驗證包含 1～8 級遍歷與固定動作清單，不會自動涵蓋新子職和高等級。
- 施法驗證固定法術總數與各環數量，並用 `git show HEAD:spell-list.js` 比較 ID 集合；合法新增法術也會觸發原預期失敗。修改前查看腳本中的預期，不另在指南維護一份數量。
- 新增資料時，精確加入新增 ID／數量／案例的預期，繼續驗證所有舊 ID 保留、ID 不重複及未授權刪改；不要刪除整個比較，也不要自動接受所有差異。
- 真正產品錯誤要修產品；設計範圍有意改變才更新相應預期。保留舊範圍回歸，再補新內容與邊界案例。
- 維持既有 Node.js／Playwright 驗證方式，不另建測試框架。環境限制時執行可做的檢查，精確列出未驗證部分。

## 9. 可直接複製的範例任務

以下是需求句型，不是新規則；方括號內容由使用者填入。

```text
新增子職：
保留目前戰士的勇士子職，另加「[名稱]」，規則如下：[逐級能力]。
只支援這兩個戰士子職到 12 級，其餘職業保持原支援範圍。
支援角色卡、跑團模式與 JSON；創角小幫手固定排除，本次也不需要 PDF 或離線產物。
舊戰士角色沒有子職欄位時沿用勇士。請補齊高等級共用計算與相容性，
不要只新增子職說明。戰場位置條件可由玩家判斷，資源次數要能追蹤。
```

```text
新增法術／戲法：
新增「[名稱]」，環階 [0 或指定環階]，職業清單 [清單]，完整術文如下：[內容]。
施法屬性及取得來源：[內容]；角色取得方式：[內容]。
需要搜尋、加入、準備、合法施法與 JSON 還原。
自動化範圍：[骰式／升環／專注／治療／只扣資源並提示]。
若超過現有四環 UI，請完成此需求所需的法術位與環階介面串接。
不要順帶擴充所有其他法術與職業。
```

```text
新增傳奇恩惠：
新增自創「[名稱]」，取得來源 [某職業升級／DM 額外授予]，
取得條件 [條件]，是否占一般專長選擇 [是／否]，是否可重複 [規則]。
能力全文：[含屬性加值上限、觸發、消耗與恢復的內容]。
支援角色卡、跑團模式與保存；需要自動化的部分是 [內容]。
若這是自創獎勵，請保留上述自訂門檻，不擅自套用其他版本的取得條件。
```

## 10. 完成時 AI 應交付什麼

- 說明實際支援的內容、等級與介面，以及保留手動處理的效果。
- 列出主要修改檔案與目的；指出對舊角色、ID、資源與分享資料的處理。
- 回報執行的檢查、結果、新增驗收案例與未驗證範圍。
- 確認沒有本次造成的未處理失敗、debug code、臨時檔或無關修改。
- 若曾使用外部規則，附精確來源、版本與自創改動範圍；不宣稱整個私人版本已通過遊戲平衡驗證。
- 只在使用者要求時提交、上傳、部署或產製離線版。單純閱讀本指南不代表授權執行其中所有工作。
